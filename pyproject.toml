[project]
name = "dptech-exporter"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "prometheus-client>=0.23.1",
    "pysnmp>=7.1.22",
    "pysnmpcrypto>=0.1.0",
    "pyyaml>=6.0.3",
]
